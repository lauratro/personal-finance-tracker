import styled from 'styled-components';

export const AiChatIconContainer = styled.div`
  position: fixed;
  z-index: 1000;
  bottom: 20px;
  right: 20px;
  border-radius: 50%;
  background-color: var(--primary);
  padding: 10px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
`;

export const AiChatIconButton = styled.button`
  background: none;
  border: none;
  cursor: pointer;
`;
