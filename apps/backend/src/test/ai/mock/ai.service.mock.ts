export const mockAiService = {
  generateWithTools: jest.fn(),
  generateAfterToolCall: jest.fn(),
};

export const mockGetNetWorthsService = {
  execute: jest.fn(),
};

export const mockListInvestmentsService = {
  execute: jest.fn(),
};
